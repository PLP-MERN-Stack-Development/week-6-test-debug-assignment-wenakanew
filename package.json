{
  "name": "mern-testing-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm --prefix client install && npm --prefix server install",
    "test": "npm --prefix client test && npm --prefix server test",
    "test:unit": "npm --prefix client run test:unit && npm --prefix server run test:unit",
    "test:integration": "npm --prefix client run test:integration && npm --prefix server run test:integration",
    "test:e2e": "npm --prefix client run test:e2e"
  }
} 